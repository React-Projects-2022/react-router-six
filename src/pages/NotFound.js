import { NavLink } from "react-router-dom";

const NotFound = () => (
    <>
        <h1>NotFound</h1>
        <p>NotFound layout</p>
        <NavLink to="/">Volver a la página inicial</NavLink>
    </>
);

export default NotFound;